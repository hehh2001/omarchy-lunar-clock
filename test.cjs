const fs=require('node:fs'),vm=require('node:vm'),assert=require('node:assert/strict');
const ctx={};vm.createContext(ctx);vm.runInContext(fs.readFileSync(__dirname+'/LunarData.js','utf8'),ctx);ctx.Data={months:ctx.months};vm.runInContext(fs.readFileSync(__dirname+'/Lunar.js','utf8').replace(/^\.import.*\n/,''),ctx);
for(const [s,m,d,l,f] of [['2026-02-17',1,1,false,'春节'],['2026-06-19',5,5,false,'端午'],['2026-09-25',8,15,false,'中秋'],['2025-07-25',6,1,true,''],['2026-02-16',12,29,false,'除夕']]){const dt=new Date(s),r=ctx.date(dt.getUTCFullYear(),dt.getUTCMonth(),dt.getUTCDate());assert.deepEqual([r.month,r.day,r.leap,r.festival],[m,d,l,f]);}
let n=0;for(const line of fs.readFileSync(__dirname+'/hko-2026.csv','utf8').trim().split(/\r?\n/).slice(1)){const [s,gan,zodiac,month,day]=line.split(',');const dt=new Date(s+' 12:00:00 UTC'),r=ctx.date(dt.getUTCFullYear(),dt.getUTCMonth(),dt.getUTCDate());assert.equal(ctx.dayName(r.day),day,s);assert.equal((r.leap?'閏':'')+['正月','二月','三月','四月','五月','六月','七月','八月','九月','十月','十一月','十二月'][r.month-1],month,s);n++;}
for(let t=Date.UTC(1900,0,1);t<Date.UTC(2101,0,1);t+=86400000){const d=new Date(t),r=ctx.date(d.getUTCFullYear(),d.getUTCMonth(),d.getUTCDate());assert(r.day>=1&&r.day<=30);assert(r.month>=1&&r.month<=12);}
assert.equal(ctx.date(1899,11,31),null);assert.equal(ctx.date(2101,0,1),null);console.log('PASS: '+n+' HKO dates, festival/leap/NYE fixtures, all 1900–2100 dates and range boundaries');
